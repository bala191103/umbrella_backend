from django.apps import AppConfig


class ProductReviewConfig(
    AppConfig
):
    default_auto_field = (
        "django.db.models.BigAutoField"
    )

    name = (
        "modules.product_review"
    )