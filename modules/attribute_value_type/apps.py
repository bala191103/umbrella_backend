from django.apps import AppConfig


class AttributeValueTypeConfig(
    AppConfig
):

    default_auto_field = (
        "django.db.models.BigAutoField"
    )

    name = (
        "modules.attribute_value_type"
    )